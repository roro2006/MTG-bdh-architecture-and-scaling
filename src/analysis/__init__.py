"""Probes that read a trained checkpoint rather than producing one.

Nothing here trains, and nothing here is on the path of a grid run. These
are the measurements that say what a trained model actually learned, as
opposed to how well it scored.
"""
