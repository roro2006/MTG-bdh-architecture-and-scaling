# Training and analysis

The grid runner (sweeping model size × dataset fraction × architecture × seed), the curve-fitting code for $L(N, D) = E + A/N^\alpha + B/D^\beta$, and the matched-state disagreement measurement used to validate the fitted $E$ against a real human Bayes-error floor.
