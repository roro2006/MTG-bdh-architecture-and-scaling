[pytest]
testpaths = tests
addopts = -q --strict-markers
markers =
    slow: takes more than a few seconds (model init and JAX compilation)
    integration: exercises several modules together rather than one in isolation
